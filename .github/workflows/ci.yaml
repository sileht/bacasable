name: Continuous Integration

on:
  push:

jobs:
  simple-self:
    runs-on: ubuntu-24.04
    steps:
      - name: Before
        run: |
          echo "$GITHUB_JOB"
          echo "$GITHUB_JOB_ID"
          echo "$GITHUB_JOB_NAME"
          echo "$RUNNER_NAME"
          echo "$RUNNER_ID"
          echo "$RUNNER_ID"
      - uses: qoomon/actions--context@v4
      - name: Tests
        run: |
          echo "$GITHUB_JOB"
          echo "$GITHUB_JOB_ID"
          echo "$GITHUB_JOB_NAME"
          echo "$RUNNER_NAME"
          echo "$RUNNER_ID"
