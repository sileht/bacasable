name: Testing thing
on:
  pull_request:

jobs:
  foobar_id:
    name: foobar_name
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        foo: [bar, foo]
        os: [linux, macos]
    steps:
      - name: Get job name
        run: |
          # Both work:
          echo "Job name is $GITHUB_JOB"
          echo "How about ${{ github.jobs[github.job].name }}"
